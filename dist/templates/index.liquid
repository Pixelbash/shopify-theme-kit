<section class="content content-collection">
  {% paginate collections.featured.products by 24 %}  

  <div class="blocks blocks-product">
    {% for product in collections.featured.products %}
      {% include 'block-product' %}
    {% endfor %}
  </div>

  {% endpaginate %}
</section>