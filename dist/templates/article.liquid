<section class="hero">
  <div class="title">
    <h1>{{ article.title }}</h1>
  </div>
</section>

<section class="content content-article">
  <div class="date">{{ article.published_at | date: '%d-%m-%Y' }}</div>
  <div class="content">
    {{ article.content }}
  </div>
</section>